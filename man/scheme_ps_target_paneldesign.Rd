% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/repdata_design_attribs.R
\name{scheme_ps_target_paneldesign}
\alias{scheme_ps_target_paneldesign}
\title{Target panel designs per scheme x panel set x module combo}
\format{
Data frame.
}
\source{
The REP (revisitplan) workflow. See
\url{https://github.com/inbo/n2khab-mne-designs/tree/revisitplan/100_design_common/010_revisitplan}.
}
\description{
The data frame \code{scheme_ps_target_paneldesign} contains only the panel designs
(from \code{\link{mod_scheme_ps_fag_paneldesign}}) for the so called 'target field
activity groups' (target FAGs), i.e. the FAGs containing the target field
activity (see \code{\link{mod_scheme_targetfieldactivity}}). These panel designs can be
called \emph{target} panel designs and the corresponding panels will be called
'targetpanels'. Importantly, the module level is gone: instead, modules with
the same target panel design are combined as 'module combos' at the scheme x
panel set level.
}
\details{
\code{scheme_ps_target_paneldesign} is derived from
\code{\link{mod_scheme_targetfieldactivity}}, \code{\link{mod_scheme_ps_fag_paneldesign}} and
\code{\link{mod_scheme_field_activity}} (to link field activity and field activity
groups).

The target variables limit the main field activities of a module x scheme to
a great extent, since the latter also comprise variables for explanation &
quality control. (Remember: the term 'main field activity' is to be
understood from the viewpoint of a variable (set) in a module x scheme, not
just the module x scheme; see \code{\link{mod_scheme_variableset_mainfieldactivity}}.)

For each scheme x panel set, the modules (in
\code{\link{mod_scheme_ps_fag_paneldesign}}) have been collapsed to \strong{module combos}
(module combinations) if the corresponding target panel design is the same.
\itemize{
\item So, beware that the module combo can only be interpreted as a unique panel
design in combination with scheme x panel set.
\item \code{scheme_ps_target_paneldesign} is the object in the REP-workflow where the
module combos are effectively generated and defined.
\item However, see the derived data frame \code{\link{mod_scheme_yrs_moco_ps}} for a
convenient object that links modules and module combos within scheme x panel
set.
\item The concept of module combos is important to the spatial
sampling procedure: spatial sampling for a scheme x panel set is to be
combined over active modules when their target panel design is the same. This
is necessary since the modules can have overlapping domains.
}

Note that the 'spatial subset' from \code{\link{mod_scheme_ps_fag_paneldesign}} is
gone. That is because for the \emph{main} field activities the spatial subsets
don't have differences in the corresponding panel designs.
}
\seealso{
Other REP objects with design attributes:
\code{\link{mod_scheme_ps_fag_paneldesign}},
\code{\link{mod_scheme_yrs_moco_ps}},
\code{\link{module_domain_scheme_ps_stratum_sample_size}},
\code{\link{scheme_ps_variableset_paneldesign}},
\code{\link{submodule_domain_scheme_ps_designattr}}
}
\concept{REP objects with design attributes}
